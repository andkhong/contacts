import * as React from 'react';

function Footer(){
    return (
        <div className="Footer">
            <button> + </button>
            <button> Edit </button>
        </div>
    );
}

export default Footer;
