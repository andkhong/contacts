export interface SelectedProps {
    selected: {
        id: number;
        firstName: string;
        lastName: string;
        email: string;
        bDay: string;
        mobile: string;
        address: string;
    };
}